# Python
__pycache__/
*.pyc

# Model binaries — large; download from Google Drive (see README) or rebuild
# with scripts/compile_model.sh. Only the folder placeholder is tracked.
/assets/models/*
!/assets/models/.gitkeep

# Compile intermediates (regenerated by scripts/compile_model.sh)
/compile/build/
/compile/compile_report.json

# Tracked as demo assets: sample_input_images/ and the annotated output_images/.
